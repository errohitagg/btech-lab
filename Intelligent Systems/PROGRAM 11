domains
integerlist=integer*
predicates
del(symbol,integer,symbol)
start(symbol)
parse(integerlist)
final(symbol)
apply(symbol,integerlist)
clauses
del(a,0,a).
del(a,1,b).
del(b,0,a).
del(b,1,c).
del(c,1,c).
del(c,0,a).
start(a).
final(c).
parse(L):-
start(S),
apply(S,L).
apply(Q,[H|T]):-
del(Q,H,N),
apply(N,T).
apply(Q,[]):-
final(Q).
goal
parse([1,1,0,0,1,1]).
